module.exports = {
    name: 'hi',
    description: 'meow',
    // devOnly: Boolean,
    testOnly: true,
    // options: Object[],
    // deleted: Boolean,
  
    callback: (client, interaction) => {
      interaction.reply(`hiii!!! :333 hello!!! haiiii :pppp mraow mraow :333`);
    },
  };